

int main() {
    int *arr;
    int n = 5;

    arr = (int *)malloc(5 * sizeof(int));

    printf("Allocated array elements:\n");
    for (int i = 0; i < 5; i++) {
        scanf("%d ", &arr[i]);
        printf("%d ", arr[i]);
    }
    printf("\n");
    

    free(arr);

    arr = nullptr;

    printf("Memory successfully deallocated.\n");
    return 0;
}