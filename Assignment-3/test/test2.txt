#include <stdio.h>

int main() {
    long a = 5 , b ,aa , bb ,cc , dd,ff,gg;
    cin>>b;
    int c= a+b;
    int d =a-b;
    int e = a*b ;
    int f=a/b;
    while(b < 0) b++;
    int g =a%b;
    c*=d;
    if(!e) d/=e;

    f|= a;
    g ^=c;
    e&=99999;

    a>>=2;
    b<<=4;

    cout<< (g ==9? a >>3: b<<2)<<"\n";

    until(b < 0) b--;
    int s = sizeof(a);

    a+= 5 ;
    b -=6;
    g = b;

    dd =sizeof(f);
    bb = a|b;
    cc=a&b;
    dd= a^b ;
    long ee=~e;

    // yahoo
    
    return 0;
}