#include <stdio.h>

int main() {
    int a = 5 , b = 7;

    if(a>b) {
        cout<<"Bada Hai"<<"\n";
    }
    else if(a <b) {
        cout<<"Chota Hai"<<"\n" ;
    }
    else if(a==b) {
        cout<<"Saman Hai"<<"\n";
    }

    if(a>= b) cout<<"Sure" <<"\n";
    

    if(a <=b) {
        cout<< "Not Sure"<<"\n";

        if(a <b && a== b ) cout<<"Is it possible"<<"\n";
    }

    if(a !=b) cout<<"Not Equal"<<"\n";
    if(a==b || a!=b) cout<<"To be Printed"<<"\n";
    
    // yeh switch case
    char x = 'I';
    switch(x) {
    case 'A'{
        cout << "A";
        break;
    }
    case 'B'{
        cout << "B";
        break;
    }
    default{
        cout << "Other than A and B";
    }
    }

    goto skipItAll;

    int a = **p;
    int pd = &(&p);
    double uk = 6.9;
    skipItAll:
    int count = 8;
    while(count>0) count--;
    count = -9;
    do {
        count++;
    } while(count<0);
    count =8;
    until(count == 3) count--;

    return 0;
}